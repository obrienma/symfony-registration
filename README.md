# symfony-registration

# 1. Clone the repo
git clone symfony-registration
cd symfony-registration

# 2. Start Docker containers
docker-compose up -d

# 3. Install dependencies from composer.lock
docker-compose exec php composer install